# Production runtime dependencies — pinned to the exact versions the test
# suite was verified against (22/22 passing). Dev/test tools live in
# requirements-dev.txt so the production image stays lean.
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
